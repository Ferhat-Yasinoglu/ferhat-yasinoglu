node_modules/
dist/
*.log
.env
.env.local
.DS_Store

# Your live rules, which usually carry prices and links you edit as you go.
rules.json

# What the web panel writes: access tokens, app secrets, the Claude key. This
# must never reach the repository.
data/
