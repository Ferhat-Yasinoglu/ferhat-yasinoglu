# Never bake credentials into an image. Two files must not travel with the
# build: the .env you run locally, and whatever the web panel has saved.
.env
.env.local
data/

node_modules
dist
test
*.log
.git
.github
README.md
